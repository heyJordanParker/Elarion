namespace Elarion.UI {
    public class UILoadingScreen : UIScreen {
        
        // TODO inherit this class or initialize it with a loading function and use it as a loading screen (aka a screen that auto transitions to another screen when the function returns true) 
        
        // Old to do add a WaitFor function input; pause the transition at 50% and don't resume it until the WaitFor function returns true; this will enable this to be used for loading & showing new scenes and such

    }
}