namespace Elarion.StateMachine {
	//TODO remove this enumeration and move the camera states to a more clearer logic using FSMs
	public enum UpdateType {
		Update = 0,
		LateUpdate,
		FixedUpdate,
	}
}