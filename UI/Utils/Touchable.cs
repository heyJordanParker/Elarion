using UnityEngine.UI;

namespace Elarion.UI.Utils {
    /// <summary>
    /// Use this instead of a graphic object to create an invisible button without the overhead of a transparent layer
    /// </summary>
    public sealed class Touchable : Text { }
}